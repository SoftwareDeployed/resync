let event_url = "/_events";
/* Use empty string for base_url to make WebSocket use the same host/port as the page.
   This is required for browser tests which run on a dynamic port. */
let base_url = "";
