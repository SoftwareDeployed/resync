(executable
 (name discover)
 (modules discover)
 (libraries dune-configurator)
 (enabled_if (= %{os_type} "Unix")))

(rule
  (targets c_flags.sexp c_libs.sexp icu_version.ml)
  (deps (:discover ./discover.exe))
  (action (run %{discover}))
  (enabled_if (= %{os_type} "Unix")))

(library
 (name icu_numberformatter)
 (public_name resync.icu_numberformatter)
 (libraries ctypes.foreign integers)
 (wrapped false)
 (modules Icu Icu_version Number_formatter)
   (c_library_flags (:include c_libs.sexp)))
