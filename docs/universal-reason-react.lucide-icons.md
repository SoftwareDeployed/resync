# universal-reason-react/lucide-icons

Universal Lucide icon rendering for server and client.

- Keeps icon output consistent across SSR and hydration.
- Lets apps use a shared icon layer instead of re-solving SVG rendering per runtime.

Use this package when you want:

- matching server/client icon markup
- a shared icon abstraction for universal Reason React apps

This package is part of the current prototype stack and its API is still subject to change.
This document is AI-generated and should be reviewed and edited by a human before being considered final.
